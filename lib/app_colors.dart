import 'package:flutter/material.dart';

class AppColors {
  // Prevent instantiation
  AppColors._();

  static const Color primary = Color(0xFF001668); // Navy (Primary)
  static const Color accent = Color(0xFFFD8A02); // Orange (Accent)
  static const Color alert = Color(0xFFFF0000); // Red (Alert)
  static const Color secondary = Color(0xFF4F0049); // Plum (Secondary)

  // Variabel tambahan jika dibutuhkan untuk gradient Plum
  static const Color secondaryLight = Color(0xFF7B006B);
}
